## Integration tests

The integration test generates a tiny GLB, runs `wmx build`, and asserts the expected output folder layout and manifest validity.

From repo root:

```bash
npm install
npm run test:integration
```

